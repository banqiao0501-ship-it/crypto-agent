# === 複製這個檔案為 .env，並填入你自己的金鑰 ===
# .env 不要commit進git，裡面是機密資訊

# --- Anthropic Claude API ---
# 申請：https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# --- CoinGecko API ---
# 免費 Demo API Key 申請：https://www.coingecko.com/en/developers/dashboard
# 沒有key時部分公開端點仍可能可用，但穩定性較差，建議申請
COINGECKO_API_KEY=

# --- LINE Messaging API ---
# 現在的流程（2024年9月後LINE改過，不能再直接從Developers Console建立Channel）：
# 1. 先到 https://manager.line.biz 建立一個LINE官方帳號（免費，選「稍後認證」即可）
# 2. 在官方帳號後台「設定」→「Messaging API」，點「啟用Messaging API」，選/建立一個Provider
# 3. 啟用後切到 https://developers.line.biz/console，同一個Provider底下會看到這個Channel，
#    點進去、「Messaging API」分頁最下面「Channel access token」點「Issue」產生長期token
# 4. 同一個Channel的「Basic settings」分頁，往下捲會看到「Your user ID」，就是你自己的User ID
# 5. 別忘了用手機LINE掃描「Messaging API」分頁上方的QR code，把這個官方帳號加為好友，
#    不然Push訊息送出成功你手機也收不到
LINE_CHANNEL_ACCESS_TOKEN=
LINE_USER_ID=

# --- 資料庫 ---
DATABASE_PATH=data/crypto.db

# --- 日誌 ---
LOG_LEVEL=INFO
LOG_PATH=logs/crypto-agent.log
